# FRAMS - Facial Recognition Attendance Management System
# Install on Raspberry Pi OS (Bullseye / Bookworm):
#   pip install -r requirements.txt
#
# Note: opencv-contrib-python pulls in the LBPH face recogniser.
# On Raspberry Pi, install the headless variant to avoid X11 conflicts:
#   pip install opencv-contrib-python-headless
# On a desktop/dev machine with a display, use:
#   pip install opencv-contrib-python

# --- Computer Vision ---
opencv-contrib-python-headless>=4.8.0

# --- Camera (Raspberry Pi Camera Module 2) ---
# picamera2 is pre-installed on Pi OS Bullseye+.
# Include here so it appears in pip freeze / documentation.
picamera2>=0.3.12

# --- Hardware / GPIO ---
RPi.GPIO>=0.7.1
gpiozero>=2.0

# --- I2C / Peripheral Communication ---
smbus2>=0.4.2          # Low-level I2C for DS3231 RTC and LCD
RPLCD>=1.3.0           # High-level 16x02 LCD driver (I2C / GPIO)

# --- Database ---
# sqlite3 is part of Python's standard library — no pip install needed.

# --- Web Dashboard ---
Flask>=3.0.0
Flask-Cors>=4.0.0      # Required if the MJPEG stream is consumed cross-origin

# --- Report Export ---
openpyxl>=3.1.2        # Excel (.xlsx) export
pandas>=2.1.0          # DataFrame used for CSV / Excel report generation

# --- Utilities ---
Pillow>=10.0.0         # Image handling for dataset preprocessing
requests>=2.31.0       # HTTP client for network sync (T22)

# --- Supabase Cloud Integration ---
supabase>=2.0.0        # Official Python client (PostgreSQL + Storage)
httpx>=0.24.0          # Required by supabase-py internals
